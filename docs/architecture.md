# PFSE Architecture

1. CSV Data hosted in GitHub
2. Neo4j loads graph via LOAD CSV
3. Node.js API executes queries
4. UI consumes API

Core Engines:
- Decision Engine (strategy)
- Ontology Engine (adjacency)
- Partner Engine (SI matching)
